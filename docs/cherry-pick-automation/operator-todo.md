# Draft — local review required

# Operator TODO

The canonical queue of prohibited remote follow-up actions is
[`REMOTE_ACTIONS_TODO.md`](REMOTE_ACTIONS_TODO.md).

No item is authorized during local implementation. In particular, do not push,
open or modify a PR, apply or remove a label, comment, dispatch a workflow,
change an App/secret/ruleset, or run public CI. A human must first review the
complete local draft and explicitly authorize a later remote phase.
