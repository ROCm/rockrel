# Copyright Advanced Micro Devices, Inc.
# SPDX-License-Identifier: MIT

# Temporary dispatcher used only to validate the TSAN workflow before its new
# filename exists on rockrel's default branch. This branch is not the PR head.
name: Multi-Arch Release Host TSAN Branch Validation

on:
  workflow_dispatch:
    inputs:
      release_type:
        description: "Artifact release type."
        type: choice
        options: [dev]
        default: dev
      linux_amdgpu_families:
        description: "Linux artifact families. Tests remain CPU-only."
        type: string
        default: "all"
      ref:
        description: "TheRock branch, tag, or SHA."
        type: string
        default: "users/sa-faizal/tsan-multi-arch"

permissions:
  contents: read

run-name: >-
  Multi-Arch Release Host TSAN branch validation (${{ inputs.release_type }})
  | Linux artifacts: ${{ inputs.linux_amdgpu_families }}

jobs:
  release:
    uses: ROCm/TheRock/.github/workflows/multi_arch_release_tsan.yml@users/sa-faizal/tsan-multi-arch
    secrets: inherit
    with:
      release_type: ${{ inputs.release_type }}
      linux_amdgpu_families: ${{ inputs.linux_amdgpu_families }}
      repository: "ROCm/TheRock"
      ref: ${{ inputs.ref }}
    permissions:
      contents: read
      actions: read
      id-token: write
